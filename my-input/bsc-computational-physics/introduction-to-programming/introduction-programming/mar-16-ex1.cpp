#include <stdio.h>

main(){
       int x,y;
       int a=14, b=3;
       float z;
       x= a/b;
       y=a%b;
       z=y/x;       
       printf("%d, %d, %d", x, y, z);       
       getchar();
       }
