#include<stdio.h>
#include<stdlib.h>

int verifica(int x, int v[], int n)
{
    if(x==0) return(-1); // elemento nao encontrado // criterio de parada
    if(x==v[n-1])return(v[n-1]); //elemento encontrado // imprime ele
      
    
    
    
    
    }
    
    main(){
           int vet[5]={2,32,4,63, 17};
           
           
           
           }
