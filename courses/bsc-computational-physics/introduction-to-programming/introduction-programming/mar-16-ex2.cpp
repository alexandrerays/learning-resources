#include <stdio.h>

main(){
       int x,y;
       int a=14, b=3;
       float z;
       x= a/b; //resultado 4
       y=a%b; //resto da divisào de a por b
       z=y/x;       
       printf("%d, %d, %d", x, y, z);       
       getchar();
       }
       
       //resultado x=4, y=2, z=0n
