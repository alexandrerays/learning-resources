
# ROTEIRO LAB #2 - TABLEAU

## Introdução

- explicar como baixar o tableau public / public.tableau.com

- explicar sobre os dados no excel

  - dados do banco alpha bank

- explicar como importar os dados

- conceito de: 
  - planilha (onde cria o gráfico)
  - painel (onde cria a dashboard com vários gráficos)
  - livro (conjunto de planilhas e painéis)

- tableau funciona diferente, primeiro constrói os graficos/planilhas e depois criar o painel com vários gráficos. Se quiser 3 graficos, crio 3 planilhas uma para cada gráfico.

- explicar os principais elementos da ferramenta (seções da ferramenta)

---

## Exemplos de gráficos com o dataset de exemplo

### **GRÁFICO #1**

* renda média por idade / explicar dimensão (o que quero agrupar) e medida (como quero agrupar)


> arrastar `age` para `colunas` e colocar como `dimensão` e `discreto`
>
> arrastar `income` para `linhas` e colocar como `medida (média)` e `contínuo` 
> 
> dois cliques no `eixo y` para renomeá-lo + trocar o `título da planilha`
> 



### **GRÁFICO #2** 

* qtde de pessoas por escolaridade


> arrastar `Education` para `Colunas` e colocar como `dimensão` e `discreto`
> 
> arrastar `ID` para `linhas` e colocar como `medida (contagem distinta)` e `contínuo`
> 
> clicar com botão direito no `eixo x` e em `editar alias` para editar os nomes das colunas







### **GRÁFICO #3**

* mapa

> arrastar `longitude` para as `colunas`
> 
> arrastar `latitude` para as `linhas` para formar o mapa
> 
> arrastar `Zip Code` para `Detalhe` para mostrar quais zip codes temos na base
> 
> arrastar `Income` para `Cor` e editar a cor




### **Montar painel**

> Criar um `painel`
> 
> mostrar como fazer fazer filtro. Clicar na `setinha` > `Filtros` > `Age`
> 
> painel - visualização diferente para cada tipo de dispositivo (celular/tablet)
> 
> Mostrar `dica de ferramenta`



### **GRÁFICO (opcional)**

* quem usa vs quem não usa cartão de crédito





### Obs

* filtro travado na origem vs filtro que o usuário pode fazer
